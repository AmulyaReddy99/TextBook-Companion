# page number: 191

x<-runif(100)
y<-runif(100)
plot(x,y,main="White blood cells")

cat("With parameter λ = 40, it is easier to model the situation using the Poisson distribution than the binomial distribution")